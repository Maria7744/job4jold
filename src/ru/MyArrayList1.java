package ru.job4jj;


public class MyArrayList1<E> {

      /*  private  E[] elements;
        private int count = 0;

        public MyArrayList1() {

            elements =  (E[])new Object[1];
        }

        public void add(E value) {
            if (count < elements.length) {
                elements[count] = value;

            } else {
                E[] container = (E[])new Object[elements.length * 2];
                for (int i = 0; i < elements.length; i++) {
                    container[i]= elements[i];
                }
                container[count] = value;
                this.elements = container;
            }
            count++;

        }
        public int size(){
            return  count;
        }
        public E getAt(int index) {
            return elements[index];//возврат элемента с индексом i
        }



        // public void add() {
        //   }


        //  public void add() {
    }
public class Iterator1 extends Iterable {
    private Object cities;
    Iterator1<E> cityIterator= .iterator1();
cityIterator.next();
cities.remove(1);
cityIterator.next();

    final void checkForComodification() {
        Object expectedModCount;
        Object modCount=0;
        if (modCount != expectedModCount)
            throw new ConcurrentModificationException();
    }
}*/

}
